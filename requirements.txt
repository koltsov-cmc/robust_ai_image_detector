# Detector training/inference plus the downloaded root-level augmentations.
# The compute host driver exposes CUDA 12.4, so PyTorch/torchvision are pinned
# to the matching official cu124 wheels. The code refuses to fall back to CPU.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.5.1+cu124
torchvision==0.20.1+cu124
open_clip_torch>=3.2,<4
peft>=0.14,<1
timm>=1.0.18,<2
numpy>=1.24,<3
Pillow>=10,<13
PyYAML>=6,<7
scikit-learn>=1.4,<2
filelock>=3,<4
